Nancy MVC, Hosted with OWIN, using the Razor View Engine
========================================================

Simply run in Visual Studio or Install as a Windows Service using the command "<build path>\nancy-razor.exe install"